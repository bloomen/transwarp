sudo: false
language: cpp

matrix:
  include:
    - os: linux
      compiler: gcc
    - os: linux
      compiler: clang
#    - os: osx
#      compiler: clang

script:
    - $CXX --version
    - ./test.sh Debug
    - ./test.sh Release
	
notifications:
  email:
    on_success: change
    on_failure: change
